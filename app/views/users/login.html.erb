<div class="container">

  <div class="page-header">
    <h1>Sign in%>
      <br>
        <small>
          Blah
        </small>
    <h1>
  </div>

<!-- CURRENTLY BROKEN -->
  <%= form_for(@user) do |f| %>
    <div class="form-group">
      <label for="due" class="form-control, col-sm-2 control-label">Email</label>
      <div class="col-sm-10">
        <%= f.email_field :email, type:"email", class:"form-control, col-sm-5" %>
      </div>
    </div>

    <br></br>

    <div class="form-group">
      <label for="password" class="form-control, col-sm-2 control-label">Password</label>
      <div class="col-sm-10">
        <%= f.text_field :password, type:"text", class:"form-control, col-sm-5" %>
      </div>
    </div>
  <% end %>
    <br></br>
